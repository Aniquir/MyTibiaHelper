package com.example.storeservice.service.mountService.mountServiceHelper;

import com.example.storeservice.dto.MountDTO;

public interface MountServiceHelper {

    void createAndSave(MountDTO mountDTO);
}
